# 金融早报自动化 — Python 依赖
# 安装命令：pip install -r requirements.txt

# 市场数据
akshare>=1.12.0
yfinance>=0.2.36
# akshare 依赖 jsonpath（该包 wheel 构建有问题），用 jsonpath-ng 作为替代
# 需同时执行 setup_jsonpath_shim.py 安装兼容层
jsonpath-ng>=1.6.0

# 新闻抓取
feedparser>=6.0.10
requests>=2.31.0

# LLM / HTTP 客户端
httpx>=0.27.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
